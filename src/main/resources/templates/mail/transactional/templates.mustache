<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Templates</title>
</head>
<body>

<form
        method="POST"
        action="/mail/transactional/templates/send"
        enctype="multipart/form-data"
>

    <p>보낼 템플릿 </p>
    <select>
        {{ #templates }}
        <option value="{{ publishName }}"> {{ publishName }} </option>
        {{ /templates }}
    </select>

    <p>유저 목록 </p>
    <input
            name="file"
            type="file"
            accept=".xlsx"
            required
    />

    <button type="submit"> 발송</button>
</form>

</body>
</html>